Currently pentaho (kettle) do not support apache phoenix, this repository do some Optimization on it to support upsert and select action .  use "mvn package clean install"  to build on "pdi" project . 
